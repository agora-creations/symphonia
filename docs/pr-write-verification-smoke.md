# PR Write Verification Smoke

This is a manual PR write verification smoke change for Symphonía.

The change is intentionally small and docs-only. It exists to verify the
Symphonía flow:

Linear issue -> Codex run -> isolated worktree -> approval evidence -> review
artifact -> draft GitHub PR
